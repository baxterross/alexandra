html, body {
	height:100%;
	width:100%;
	margin:0;
	padding:0;
	border:0;
	font-family:Impact, Charcoal, sans-serif;
	background:rgba(212, 209, 194, 0);
	font-size:14px;
	color:#444;
	overflow:hidden;
}

a {
	font-weight:bold;
	color:inherit;
}
	a:hover {
		opacity:.8;
	}

/* left */
#left {
	height:100%;
	width:30%;
	position:absolute;
	top:0;
	left:0;
}
	#left img {
		position:absolute;
		bottom:0;
		left:0;
		width:100%;
		opacity:0;
		filter:alpha(opacity=0);
	}

.home {
	z-index:4;
}
	.home .menuLink {
		left:20px;
	}
.first {
	z-index:3;
}
	.first .menuLink {
		left:200px;
	}
.second {
	z-index:2;
}
	.second .menuLink {
		left:400px;
	}
.third {
	z-index:1;
}
	.third .menuLink {
		left:600px;
	}

#right {
	float:right;
	height:80%;
	width:70%;
	padding:10% 0;
}
	.contentWrapper {
		width:100%;
		position:absolute;
	}
		.contentDrawer {
			display:none;
		}
			.contentInner {
				margin:0 0 0 40px;
				height:360px;
				width:710px;
				opacity:0;
				filter:alpha(opacity=0);
				text-align:justify;
				line-height:22px;
				overflow:hidden;
			}
				.contentInner p {
					margin:40px 10px 10px;
				}
				.contentInner .centerHolder {
					white-space:nowrap;
					margin:0 auto;
					padding:30px 0 0;
					text-align:center;
				}
					.contentInner .arrow {
						display:inline-block;
						height:240px;
						width:20px;
						cursor:pointer;
					}
						.contentInner .arrow:hover {
							opacity:.8;
						}
						.contentInner .arrow.left {
							margin-left:-30px;
						}
						.contentInner .arrow.right {
							margin-right:-30px;
						}
				.contentInner .centerContent {
					display:inline-block;
					height:240px;
					width:656px;
					padding:5px 0;
					overflow:hidden;
					vertical-align:top;
					white-space:nowrap;
				}
					.contentInner .centerContent img.headshot,
					.contentInner .centerContent div.video {
						margin:0 6px 0 4px;
						display:inline-block;
						width:150px;
						vertical-align:middle;
						border-radius:10px;
						box-shadow:2px 2px 8px 1px #444;
					}
					.contentInner .centerContent #cv {
						font-size:18px;
						color:#444;
						font-weight:bold;
					}
		.menuLink {
			position:relative;
			top:-20px;
			text-decoration:none;
			color:#000;
			text-transform:uppercase;
			font-weight:normal;
			font-size:18px;
			letter-spacing:5px;
		}
